#+TITLE: Mi Emacs
#+PROPERTY: header-args :tangle yes
#+STARTUP: showeverything
#+OPTIONS: toc:nil

* TABLE OF CONTENTS :toc:noexport:
- [[#general][General]]
  - [[#ui-basica][UI Básica]]
  - [[#archivos][Archivos]]
  - [[#historial][Historial]]
  - [[#gc-post-startup][GC post-startup]]
- [[#paquetes][Paquetes]]
- [[#apariencia][Apariencia]]
  - [[#colores][Colores]]
  - [[#fuente][Fuente]]
  - [[#tema][Tema]]
  - [[#modeline][Modeline]]
  - [[#rainbow-delimiters][Rainbow Delimiters]]
- [[#completion][Completion]]
  - [[#vertico][Vertico]]
  - [[#orderless][Orderless]]
  - [[#marginalia][Marginalia]]
  - [[#consult][Consult]]
  - [[#consult-eglot][Consult Eglot]]
  - [[#helpful][Helpful]]
- [[#proyectos][Proyectos]]
- [[#git][Git]]
- [[#desarrollo][Desarrollo]]
  - [[#direnv][Direnv]]
  - [[#lsp-eglot][LSP (Eglot)]]
  - [[#breadcrumb][Breadcrumb]]
  - [[#yasnippet][Yasnippet]]
  - [[#completion-inline-corfu][Completion Inline (Corfu)]]
  - [[#snippets-tempel][Snippets (Tempel)]]
  - [[#debug-dap][Debug (DAP)]]
  - [[#rust-rustic][Rust (Rustic)]]
- [[#ventanas-y-workspaces][Ventanas y Workspaces]]
  - [[#ace-window][Ace Window]]
  - [[#tab-bar-workspaces][Tab Bar (Workspaces)]]
  - [[#ewm][EWM]]
- [[#misc][Misc]]
  - [[#dashboard][Dashboard]]
  - [[#eat][Eat]]
  - [[#sudo-edit][Sudo Edit]]
- [[#lectura][Lectura]]
  - [[#pdf][PDF]]
  - [[#epub][EPUB]]
- [[#org][Org]]
  - [[#imagenes][imágenes]]
* General
** UI Básica
#+begin_src emacs-lisp
  (add-to-list 'default-frame-alist '(fullscreen . maximized))

  ;; Transparencia del frame
  (let ((opacity 100))
    (set-frame-parameter nil 'alpha-background opacity)
    (add-to-list 'default-frame-alist `(alpha-background . ,opacity)))

  (setq inhibit-startup-message t
        use-short-answers t
        frame-resize-pixelwise t)

  (tool-bar-mode -1)
  (scroll-bar-mode -1)
  (menu-bar-mode -1)
  (set-fringe-mode 12)
  (column-number-mode)
  (delete-selection-mode 1)
  (electric-pair-mode 1)
  (save-place-mode)
  (global-display-line-numbers-mode 1)
  (blink-cursor-mode -1)

  ;; Scroll más suave
  (setq scroll-conservatively 101
        mouse-wheel-scroll-amount '(1 ((shift) . 1))
        mouse-wheel-progressive-speed nil
        mouse-wheel-follow-mouse t)

  ;; Desactivar números de línea en ciertos modos
  (dolist (mode '(term-mode-hook
                  shell-mode-hook
                  eshell-mode-hook
                  pdf-view-mode-hook))
    (add-hook mode (lambda () (display-line-numbers-mode 0))))

  ;; C-w para cortar texto (kill-region)
  (global-set-key (kbd "C-w") #'kill-region)
#+end_src

** Archivos
#+begin_src emacs-lisp
  (setq make-backup-files nil
        auto-save-default nil
        create-lockfiles nil
        custom-file (locate-user-emacs-file "custom-vars.el"))
  (load custom-file 'noerror 'nomessage)

  ;; desktop-save-mode se encarga del restore al arrancar (after-init-hook).
  ;; desktop-save nil desactiva el guardado automático al cerrar —
  ;; solo se guarda cuando se usa C-c q explícitamente.
  (desktop-save-mode 1)
  (setq desktop-save nil
        desktop-dirname (locate-user-emacs-file "")
        desktop-restore-frames t
        desktop-load-locked-desktop t)

  (defun user/save-and-quit ()
    "Guarda la sesión de desktop y cierra Emacs."
    (interactive)
    ;; Matar procesos de eat para que no pregunte al cerrar
    (dolist (buf (buffer-list))
      (when (eq (buffer-local-value 'major-mode buf) 'eat-mode)
        (let ((proc (get-buffer-process buf)))
          (when proc (delete-process proc)))))
    (desktop-save (locate-user-emacs-file "") t)
    (save-buffers-kill-terminal))

  (defun user/clean-quit ()
    "Borra la sesión guardada y cierra Emacs (arranca desde cero la próxima vez)."
    (interactive)
    (let ((f (desktop-full-file-name)))
      (when (file-exists-p f)
        (delete-file f)))
    (save-buffers-kill-terminal))

  (global-set-key (kbd "C-c q") #'user/save-and-quit)
  (global-set-key (kbd "C-x C-c") #'user/clean-quit)
#+end_src

** Historial
#+begin_src emacs-lisp
  (recentf-mode 1)
  (setq history-length 25)
  (savehist-mode 1)
#+end_src

** GC post-startup
#+begin_src emacs-lisp
  (add-hook 'after-init-hook
            (lambda ()
              (setq gc-cons-threshold (* 1024 1024 16)
                    gc-cons-percentage 0.1)
              (setq file-name-handler-alist file-name-handler-alist-original)
              (makunbound 'file-name-handler-alist-original)))
#+end_src

* Paquetes
#+begin_src emacs-lisp
  (require 'package)
  (package-initialize)
  (require 'use-package)
  (setq use-package-always-ensure nil) ;; Nix instala los paquetes
#+end_src

* Apariencia
** Colores
#+begin_src emacs-lisp
  ;; Paleta Catppuccin Mocha hardcodeada
  (setq user/bg-0    "#11111B"   ; crust — fondo más oscuro
        user/bg-1    "#181825"   ; mantle — fondo principal
        user/bg-2    "#1e1e2e"   ; base — fondo más claro
        user/acc-0   "#CBA6F7"   ; mauve — acento principal
        user/acc-1   "#89B4FA"   ; blue — acento secundario
        user/acc-2   "#cdd6f4")  ; text

  ;; Aplicar colores a faces de UI
  (add-hook 'after-init-hook
            (lambda ()
              (set-face-attribute 'default nil :background user/bg-1)
              (set-face-attribute 'fringe nil :background user/bg-1)
              (set-face-attribute 'line-number nil :background user/bg-1)
              (set-face-attribute 'line-number-current-line nil
                                  :background user/bg-1
                                  :foreground user/acc-0)
              (set-face-attribute 'mode-line nil :background user/bg-0)
              (set-face-attribute 'mode-line-inactive nil :background user/bg-0)
              (set-face-attribute 'vertical-border nil
                                  :background user/bg-0
                                  :foreground user/bg-0)))

  ;; Minibuffer con fondo más oscuro
  (add-hook 'minibuffer-setup-hook
            (lambda ()
              (face-remap-add-relative 'default `(:background ,user/bg-0))
              (face-remap-add-relative 'fringe  `(:background ,user/bg-0 :foreground ,user/bg-0))))
#+end_src

** Fuente
#+begin_src emacs-lisp
  (set-face-attribute 'default nil
                      :font "FiraCode Nerd Font"
                      :height 130
                      :weight 'medium)
  (set-face-attribute 'variable-pitch nil
                      :font "Roboto"
                      :height 130
                      :weight 'medium)
  (set-face-attribute 'fixed-pitch nil
                      :font "FiraCode Nerd Font"
                      :height 130
                      :weight 'medium)
  (set-language-environment "UTF-8")
  (setq-default line-spacing 0)
#+end_src

** Tema
#+begin_src emacs-lisp
  (use-package doom-themes
    :config
    (setq doom-themes-enable-bold t
          doom-themes-enable-italic t)
    (load-theme 'doom-dracula t)
    (doom-themes-org-config))
#+end_src

** Modeline
#+begin_src emacs-lisp
  (use-package doom-modeline
    :init (doom-modeline-mode 1)
    :config
    (display-battery-mode 1)
    (display-time-mode 1)
    :custom (doom-modeline-height 15))
#+end_src

** Rainbow Delimiters
#+begin_src emacs-lisp
  (use-package rainbow-delimiters
    :hook (prog-mode . rainbow-delimiters-mode))
#+end_src

* Completion
** Vertico
#+begin_src emacs-lisp
  (use-package vertico
    :init
    (vertico-mode 1)

    :custom
    (vertico-cycle t)
    (enable-recursive-minibuffers t))
#+end_src

** Orderless
#+begin_src emacs-lisp
  (use-package orderless
    :custom
    (completion-styles '(orderless basic))
    (completion-category-overrides
     '((file (styles basic partial-completion)))))
#+end_src

** Marginalia
#+begin_src emacs-lisp
  (use-package marginalia
    :after vertico
    :init
    (marginalia-mode 1))
#+end_src

** Consult
#+begin_src emacs-lisp
  (use-package consult
    :bind (("C-x b"   . consult-buffer)
           ("C-x C-f" . find-file)
           ("C-c r"   . consult-recent-file)
           ("C-s"     . consult-line)
           ("C-c i"   . consult-imenu)
           :map minibuffer-local-map
           ("C-r" . consult-history)))
#+end_src

** Consult Eglot
;; Permite buscar símbolos (funciones, structs, traits, etc.) en todo el
;; workspace usando el LSP. Equivalente a Ctrl+T en VSCode.
#+begin_src emacs-lisp
  (use-package consult-eglot
    :after (consult eglot)
    :bind (:map eglot-mode-map
                ("C-c l s" . consult-eglot-symbols)))
#+end_src

** Helpful
#+begin_src emacs-lisp
  (use-package helpful
    :bind
    ([remap describe-function] . helpful-callable)
    ([remap describe-command]  . helpful-command)
    ([remap describe-variable] . helpful-variable)
    ([remap describe-key]      . helpful-key))
#+end_src

* Proyectos
#+begin_src emacs-lisp
  (use-package which-key
    :init (which-key-mode)
    :diminish which-key-mode
    :config (setq which-key-idle-delay 0.5))

  (use-package projectile
    :diminish projectile-mode
    :config (projectile-mode)
    :custom (projectile-completion-system 'default)
    :bind-keymap ("C-c p" . projectile-command-map)
    :init
    (when (file-directory-p "~/projects")
      (setq projectile-project-search-path '(("~/projects/" . 2)))))
#+end_src

* Git
#+begin_src emacs-lisp
  (use-package magit
    :custom
    (magit-display-buffer-function
     #'magit-display-buffer-same-window-except-diff-v1))
#+end_src

* Desarrollo
** Direnv
;; envrc aplica las variables de direnv por buffer en lugar de globalmente.
;; Así cada buffer ve el entorno de su propio devshell, lo que es clave
;; cuando tenés varios proyectos con distintos flakes abiertos al mismo tiempo.
;; Tiene que ir antes de eglot y dap para que vean los binarios del devshell.
#+begin_src emacs-lisp
  (use-package envrc
    :init (envrc-global-mode 1))
#+end_src

** LSP (Eglot)
#+begin_src emacs-lisp
  (use-package eglot
    :hook
    ;; Corre después de que envrc cargó el entorno
    (hack-local-variables . eglot-ensure)

    :custom
    (eglot-autoshutdown t)
    (eglot-sync-connect nil)
    (eglot-confirm-server-initiated-edits nil)

    :config
    ;; rust-analyzer: insertar paréntesis automáticamente al completar funciones.
    ;; Cuando seleccionás una función del popup, agrega fn() con el cursor adentro.
    ;; Requiere yasnippet activo para expandir el snippet que manda el LSP.
    (setq-default eglot-workspace-configuration
                  '(:rust-analyzer
                    (:completion
                     (:callable
                      (:snippets "add_parentheses")))))

    :bind (:map eglot-mode-map
                ("C-c l r" . eglot-rename)
                ("C-c l a" . eglot-code-actions)
                ("C-c l f" . eglot-format-buffer)
                ("M-."     . xref-find-definitions)
                ("M-?"     . xref-find-references)
                ("M-,"     . xref-go-back)))
#+end_src

** Breadcrumb
;; Muestra en qué función/struct/módulo estás en el header del buffer.
;; Equivalente al breadcrumb de VSCode.
#+begin_src emacs-lisp
  (use-package breadcrumb
    :hook (eglot-managed-mode . breadcrumb-local-mode))
#+end_src

** Yasnippet
;; Requerido por eglot para expandir los snippets que manda el LSP.
;; Sin esto, completar funciones no agrega paréntesis aunque rust-analyzer
;; los envíe. No hace falta configurar snippets propios — tempel se encarga
;; de eso. Yasnippet acá actúa solo como expansor para el LSP.
#+begin_src emacs-lisp
  (use-package yasnippet
    :init (yas-global-mode 1))
#+end_src

** Completion Inline (Corfu)
#+begin_src emacs-lisp
  (use-package corfu
    :init (global-corfu-mode 1)
    :custom
    (corfu-auto t)
    (corfu-auto-delay 0.2)
    (corfu-auto-prefix 2)
    (corfu-cycle t)
    (corfu-quit-no-match t))

  (use-package cape
    :init
    (add-hook 'prog-mode-hook
              (lambda ()
                (add-to-list 'completion-at-point-functions #'cape-file)
                (add-to-list 'completion-at-point-functions #'cape-keyword))))
#+end_src

** Snippets (Tempel)
;; Tempel es un sistema de snippets integrado con completion-at-point,
;; así que funciona con Corfu. tempel-collection agrega snippets predefinidos
;; para Rust, C++, org, y otros modos.
#+begin_src emacs-lisp
  (use-package tempel
    :bind (("M-+" . tempel-insert)
           ("M-*" . tempel-complete))
    :hook (prog-mode . tempel-abbrev-mode))

  (use-package tempel-collection
    :after tempel)
#+end_src

** Debug (DAP)
;; Usa lldb-dap como adaptador, que viene incluido en el devshell de C++.
;; envrc se encarga de que Emacs vea el binario correcto por proyecto.
#+begin_src emacs-lisp
  (use-package dap-mode
    :after eglot
    :config
    (dap-auto-configure-mode 1)
    (require 'dap-lldb)

    (setq dap-lldb-debug-program
          (list (or (executable-find "lldb-dap")
                    (executable-find "lldb-vscode")
                    "lldb-dap")))

    ;; Template de launch para C++
    (dap-register-debug-template
     "C++ LLDB"
     (list :type "lldb"
           :request "launch"
           :name "C++ LLDB"
           :program "${workspaceFolder}/build/${fileBasenameNoExtension}"
           :cwd "${workspaceFolder}"
           :args []
           :stopOnEntry nil))

    :bind (:map dap-mode-map
                ("C-c d d" . dap-debug)
                ("C-c d b" . dap-breakpoint-toggle)
                ("C-c d c" . dap-continue)
                ("C-c d n" . dap-next)
                ("C-c d i" . dap-step-in)
                ("C-c d o" . dap-step-out)
                ("C-c d q" . dap-disconnect)
                ("C-c d r" . dap-debug-restart)
                ("C-c d e" . dap-eval-thing-at-point)))
#+end_src

** Rust (Rustic)
;; rustic es un wrapper sobre rust-mode con integración nativa a cargo y soporte
;; para eglot. Le decimos explícitamente que use eglot (ya configurado arriba)
;; en vez de su propio cliente LSP interno.
;; rust-analyzer tiene que estar en el PATH del devshell — envrc se encarga de eso.
#+begin_src emacs-lisp
  (use-package rustic
    :custom
    (rustic-lsp-client 'eglot)
    (rustic-format-on-save t)

    :config
    ;; FIX: usar eglot-managed-mode-hook en vez de rustic-mode-hook.
    ;; eglot-managed-mode-hook se dispara recién cuando eglot tomó control del
    ;; buffer, así que la conexión está garantizada.
    (add-hook 'eglot-managed-mode-hook
              (lambda ()
                (when (and (derived-mode-p 'rustic-mode)
                           (fboundp 'eglot-inlay-hints-mode))
                  (eglot-inlay-hints-mode 1))))

    :bind (:map rustic-mode-map
                ("C-c c r" . rustic-cargo-run)
                ("C-c c b" . rustic-cargo-build)
                ("C-c c t" . rustic-cargo-test)
                ("C-c c c" . rustic-cargo-clippy)
                ("C-c c f" . rustic-cargo-fmt)
                ("C-c c a" . rustic-cargo-add)
                ("C-c c e" . rustic-cargo-expand)
                ("C-c c d" . eldoc-doc-buffer)))
#+end_src

* Ventanas y Workspaces
** Ace Window
;; Salta a cualquier ventana visible asignándole una letra.
;; C-u M-o: swap de ventanas. C-u C-u M-o: mover buffer a otra ventana.
#+begin_src emacs-lisp
  (use-package ace-window
    :bind
    ("M-o" . ace-window)
    :custom
    (aw-keys '(?a ?s ?d ?f ?g ?h ?j ?k ?l))
    (aw-scope 'frame)
    :config
    (set-face-attribute 'aw-leading-char-face nil
                        :foreground user/acc-0
                        :height 2.5
                        :weight 'bold))
#+end_src

** Tab Bar (Workspaces)
;; tab-bar-mode es built-in desde Emacs 27 y es lo que Doom usa bajo el capó
;; para sus workspaces. Cada tab mantiene su propio layout de ventanas y buffers.
;; Los faces usan la paleta Catppuccin Mocha definida en la sección Colores,
;; así que este bloque tiene que cargarse después de que user/bg-* estén seteadas.
#+begin_src emacs-lisp
  (use-package tab-bar
    :ensure nil   ; built-in
    :init (tab-bar-mode 1)

    :custom
    (tab-bar-show 1)                        ; ocultar la barra si hay solo 1 tab
    (tab-bar-new-tab-choice "*dashboard*")  ; abre dashboard al crear un tab nuevo
    (tab-bar-close-button-show nil)
    (tab-bar-new-button-show nil)
    (tab-bar-tab-name-function #'tab-bar-tab-name-current)

    :config
    (set-face-attribute 'tab-bar nil
                        :background user/bg-0
                        :foreground user/acc-2)
    (set-face-attribute 'tab-bar-tab nil
                        :background user/bg-2
                        :foreground user/acc-0
                        :weight 'bold)
    (set-face-attribute 'tab-bar-tab-inactive nil
                        :background user/bg-0
                        :foreground user/acc-2)

    :bind
    ("C-c TAB n"   . tab-bar-new-tab)
    ("C-c TAB d"   . tab-bar-close-tab)
    ("C-c TAB r"   . tab-bar-rename-tab)
    ("C-c TAB ]"   . tab-bar-switch-to-next-tab)
    ("C-c TAB ["   . tab-bar-switch-to-prev-tab)
    ("C-c TAB TAB" . tab-bar-switch-to-recent-tab)
    ("C-c TAB 1"   . (lambda () (interactive) (tab-bar-select-tab 1)))
    ("C-c TAB 2"   . (lambda () (interactive) (tab-bar-select-tab 2)))
    ("C-c TAB 3"   . (lambda () (interactive) (tab-bar-select-tab 3)))
    ("C-c TAB 4"   . (lambda () (interactive) (tab-bar-select-tab 4)))
    ("C-c TAB 5"   . (lambda () (interactive) (tab-bar-select-tab 5))))
#+end_src

** EWM
;; EWM usa Emacs como compositor Wayland. Los keybinds con Super (s-)
;; se interceptan antes de llegar a las apps Wayland, por lo que funcionan
;; globalmente igual que los de un WM tradicional.
#+begin_src emacs-lisp
  ;; Prefijos de secuencias de Emacs que el compositor redirige a Emacs
  ;; aunque una app Wayland tenga el foco. Solo van acá los prefijos de
  ;; comandos (C-x, M-x, etc.) — las multimedia keys y s- van en ewm-mode-map.

  (setopt ewm-surface-emulate-keys nil)


  (setopt ewm-intercept-prefixes
          '("C-x" "C-u" "C-h" "M-x"))

  ;; Dispositivos de entrada — EWM gestiona su propio teclado,
  ;; el xkb de configuration.nix no aplica acá.
  (setopt ewm-input-config
          '((touchpad :natural-scroll t
                      :tap t
                      :dwt t)
            (mouse :accel-profile "flat")
            (keyboard :repeat-delay 200
                      :repeat-rate 30
                      :xkb-layouts  "latam"
                      :xkb-options  "ctrl:nocaps")))

  (use-package ewm
    :config
    ;; Activar input method automático en campos de texto de apps Wayland
    ;; (Firefox URL bar, Discord, etc.) — sin esto no podés tipear en ellas.
    (ewm-text-input-auto-mode-enable)

    :custom
    ;; El puntero se mueve al centro de la ventana que recibe foco por teclado
    (ewm-mouse-follows-focus t)
    ;; Atenuar ventanas sin foco (1.0 = desactivado, 0.0 = invisible)
    (ewm-unfocused-alpha 0.85)

    ;; Ajustá los nombres de salida con: wlr-randr
    (ewm-output-config
     '(("eDP-1"    :x 0    :y 0 :scale 1.0)
       ("HDMI-A-1" :x 1920 :y 0 :scale 1.0)))

    :bind (:map ewm-mode-map
                ;; Navegación de ventanas (vim-style)
                ("s-h"         . windmove-left)
                ("s-l"         . windmove-right)
                ("s-k"         . windmove-up)
                ("s-j"         . windmove-down)
                ;; Mover ventanas
                ("s-H"         . windmove-swap-states-left)
                ("s-L"         . windmove-swap-states-right)
                ("s-K"         . windmove-swap-states-up)
                ("s-J"         . windmove-swap-states-down)
                ;; Splits y ventanas
                ("s-v"         . split-window-right)
                ("s-s"         . split-window-below)
                ("s-w"         . delete-window)
                ("s-="         . balance-windows)
                ;; Clipboard (s-v ocupado por split, s-c ocupado — los redefinimos)
                ("s-c"         . kill-ring-save)   ; copy
                ("s-y"         . yank)             ; paste
                ;; Buffers y proyectos
                ("s-d"         . consult-buffer)
                ("s-f"         . projectile-find-file)
                ("s-p"         . projectile-switch-project)
                ;; Apps
                ("s-t"         . eat)
                ("s-b"         . (lambda () (interactive)
                                   (start-process "firefox" nil "firefox")))
                ("s-m"         . magit-status)
                ;; Tabs / workspaces
                ("s-n"         . tab-bar-new-tab)
                ("s-<tab>"     . ewm-next-surface-buffer)
                ("s-<backtab>" . ewm-prev-surface-buffer)
                ;; Sistema
                ("s-<escape>"  . ewm-lock-session)
                ("s-<return>"  . ewm-toggle-fullscreen)))
#+end_src

* Misc
** Dashboard
#+begin_src emacs-lisp
  (use-package dashboard
    :config
    (setq dashboard-center-content t
          dashboard-vertically-center-content t
          dashboard-startup-banner 'logo
          dashboard-projects-backend 'projectile
          dashboard-projects-switch-function 'projectile-switch-project
          dashboard-items '((recents  . 5)
                            (projects . 5)
                            (bookmarks . 5)))
    ;; No arrancar dashboard si hay una sesión de desktop guardada.
    ;; desktop-save-mode la restaura primero; dashboard la sobreescribiría.
    (unless (file-exists-p (desktop-full-file-name))
      (dashboard-setup-startup-hook)))
#+end_src

** Eat
;; eat (Emulate A Terminal) es un emulador de terminal moderno y rápido para Emacs.
;; Soporta xterm-256color, mouse, imágenes inline, y se integra bien con eshell.
;; envrc se encarga de que el shell vea el entorno del devshell activo.
#+begin_src emacs-lisp
  (use-package eat
    :hook
    (eshell-load . eat-eshell-mode)

    :custom
    (eat-shell shell-file-name)

    :bind
    ("C-c t t" . eat)
    ("C-c t p" . eat-project)

    :config
    ;; Restaurar buffers de eat al cargar el desktop
    (defun user/eat-desktop-save (_desktop-dirname)
      "Guarda el directorio del buffer de eat para desktop."
      (list default-directory))

    (defun user/eat-desktop-restore (_file-name _buffer-name misc)
      "Restaura un buffer de eat desde el desktop."
      (let ((default-directory (or (car misc) default-directory)))
        (eat)))

    (add-hook 'eat-mode-hook
              (lambda ()
                (setq-local desktop-save-buffer #'user/eat-desktop-save)))

    (add-to-list 'desktop-buffer-mode-handlers
                 '(eat-mode . user/eat-desktop-restore)))
#+end_src

** Sudo Edit
#+begin_src emacs-lisp
  (use-package sudo-edit :defer t)
#+end_src

* Lectura
** PDF
;; pdf-tools reemplaza el DocView built-in con un renderizado nativo de alta
;; calidad usando poppler. Abre automáticamente cualquier %PDF.
#+begin_src emacs-lisp
  (use-package pdf-tools
    :magic ("%PDF" . pdf-view-mode)
    :hook (pdf-view-mode . pdf-view-midnight-minor-mode)
    :config
    (pdf-tools-install :no-query)
    ;; Cerrar el buffer de pdf-occur al saltar a un resultado con Enter
    (advice-add 'pdf-occur-goto-occurrence :after
                (lambda (&rest _)
                  (when-let (win (get-buffer-window "*PDF-Occur*"))
                    (delete-window win))))
    (add-to-list 'display-buffer-alist
                 '("\\*PDF-Occur\\*"
                   (display-buffer-reuse-window display-buffer-below-selected)
                   (window-height . 0.3)
                   (inhibit-same-window . t)))
    (add-hook 'pdf-occur-buffer-mode-hook
              (lambda ()
                (run-with-idle-timer 0 nil
                  (lambda ()
                    (when-let (win (get-buffer-window "*PDF-Occur*"))
                      (select-window win))))))
    :bind (:map pdf-view-mode-map
                ("j"   . pdf-view-next-line-or-next-page)
                ("k"   . pdf-view-previous-line-or-previous-page)
                ("C-+" . pdf-view-enlarge)
                ("C--" . pdf-view-shrink)))
#+end_src

** PDF Restore
#+begin_src emacs-lisp
  (use-package pdf-view-restore
    :after pdf-tools
    :hook (pdf-view-mode . pdf-view-restore-mode))
#+end_src

** EPUB
;; nov.el renderiza EPUBs con fuente variable-pitch y ancho de texto configurable.
#+begin_src emacs-lisp
  (use-package nov
    :init
    (add-to-list 'auto-mode-alist '("\\.epub\\'" . nov-mode))
    :custom
    (nov-text-width 80))
#+end_src

* Org
#+begin_src emacs-lisp
  (use-package org
    :hook (org-mode . org-indent-mode)
    :custom
    (org-return-follows-link t))

  (use-package org-modern
    :after org
    :hook (org-mode . org-modern-mode))
#+end_src

** Imágenes
;; org-download permite pegar imágenes del clipboard directamente en el .org.
;; Flujo: sacar captura con Spectacle → C-c i p → imagen guardada en ./img/ e insertada.
#+begin_src emacs-lisp
  (use-package org-download
    :after org
    :custom
    (org-download-method 'directory)
    (org-download-image-dir "./img")
    (org-download-heading-lvl nil)
    :bind (:map org-mode-map
                ("C-c i p" . org-download-clipboard)))
#+end_src
